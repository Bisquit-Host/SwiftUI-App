import SwiftUI

struct Console: View {
    var body: some View {
        VStack {
            Text("Console")
        }
    }
}

#Preview {
    Console()
        .padding()
        .glassBackgroundEffect()
}
